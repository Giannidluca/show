import { AppThemeProvider } from "./src/theme/theme.provider"

const App = () => {
    return (
        <AppThemeProvider>
            {/* here goes other providers and components/pages */}
        </AppThemeProvider>
    )
}
